{% extends 'base.html.twig' %}

{% block title %}Usuarios{% endblock %}

{% block body %}
    <h1>Usuarios</h1>

    <table class="table">
        <tbody>
            <tr>
                <th>Dni</th>
                <td>{{ usuario.dni }}</td>
            </tr>
            <tr>
                <th>Nombre</th>
                <td>{{ usuario.nombre }}</td>
            </tr>
            <tr>
                <th>Apelllidos</th>
                <td>{{ usuario.apelllidos }}</td>
            </tr>
            <tr>
                <th>Foto</th>
                <td>{{ usuario.foto }}</td>
            </tr>
            <tr>
                <th>FechaNac</th>
                <td>{{ usuario.fechaNac }}</td>
            </tr>
            <tr>
                <th>Pass</th>
                <td>{{ usuario.pass }}</td>
            </tr>
        </tbody>
    </table>

    <a href="{{ path('usuarios_index') }}">back to list</a>

    <a href="{{ path('usuarios_edit', {'dni': usuario.dni}) }}">edit</a>

    {{ include('usuarios/_delete_form.html.twig') }}
{% endblock %}
