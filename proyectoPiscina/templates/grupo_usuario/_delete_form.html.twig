<form method="post" action="{{ path('grupo_usuario_delete', {'id': grupo_usuario.id}) }}" onsubmit="return confirm('Are you sure you want to delete this item?');">
    <input type="hidden" name="_method" value="DELETE">
    <input type="hidden" name="_token" value="{{ csrf_token('delete' ~ grupo_usuario.id) }}">
    <button class="btn">Delete</button>
</form>
