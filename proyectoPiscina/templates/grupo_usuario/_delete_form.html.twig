<form method="post" action="{{ path('app_grupoUsuario_delete', {'id': grupoF.id}) }}" onsubmit="return confirm('Estás seguro/a de eliminarlo??');">
    <input type="hidden" name="_method" value="DELETE">
    <input type="hidden" name="_token" value="{{ csrf_token('delete' ~ grupoF.id) }}">
    <button class="btn">Delete</button>
</form>
