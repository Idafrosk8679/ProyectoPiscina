{% extends 'base.html.twig' %}

{% block title %}WebFotos{% endblock %}

{% block body %}
    <h1>WebFotos</h1>

    <table class="table">
        <tbody>
            <tr>
                <th>Id</th>
                <td>{{ web_foto.id }}</td>
            </tr>
            <tr>
                <th>Foto</th>
                <td>{{ web_foto.foto }}</td>
            </tr>
            <tr>
                <th>Descripcion</th>
                <td>{{ web_foto.descripcion }}</td>
            </tr>
        </tbody>
    </table>

    <a href="{{ path('web_fotos_index') }}">back to list</a>

    <a href="{{ path('web_fotos_edit', {'id': web_foto.id}) }}">edit</a>

    {{ include('web_fotos/_delete_form.html.twig') }}
{% endblock %}
